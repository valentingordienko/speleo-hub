import React from 'react';

import { Icon, TProps } from '../../Icon';
import { ReactComponent as SVG } from '../svg/Small403.svg';

export function Small403(props: TProps) {
  return (
    <Icon {...props} name="Small403">
      <SVG />
    </Icon>
  );
}
