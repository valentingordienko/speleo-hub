import React from 'react';

import { Icon, TProps } from '../../Icon';
import { ReactComponent as SVG } from '../svg/Niger.svg';

export function Niger(props: TProps) {
  return (
    <Icon {...props} name="Niger">
      <SVG />
    </Icon>
  );
}
