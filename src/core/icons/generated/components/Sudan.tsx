import React from 'react';

import { Icon, TBaseProps } from '../../Icon';
import { ReactComponent as SVG } from '../svg/Sudan.svg';

export function Sudan(props: TBaseProps) {
  return (
    <Icon {...props} name="Sudan">
      <SVG />
    </Icon>
  );
}
