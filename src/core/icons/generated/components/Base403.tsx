import React from 'react';

import { Icon, TBaseProps } from '../../Icon';
import { ReactComponent as SVG } from '../svg/Base403.svg';

export function Base403(props: TBaseProps) {
  return (
    <Icon {...props} name="Base403">
      <SVG />
    </Icon>
  );
}
