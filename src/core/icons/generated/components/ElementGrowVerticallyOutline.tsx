import React from 'react';

import { Icon, TProps } from '../../Icon';
import { ReactComponent as SVG } from '../svg/ElementGrowVerticallyOutline.svg';

export function ElementGrowVerticallyOutline(props: TProps) {
  return (
    <Icon {...props} name="ElementGrowVerticallyOutline">
      <SVG />
    </Icon>
  );
}
