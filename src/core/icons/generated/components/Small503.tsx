import React from 'react';

import { Icon, TProps } from '../../Icon';
import { ReactComponent as SVG } from '../svg/Small503.svg';

export function Small503(props: TProps) {
  return (
    <Icon {...props} name="Small503">
      <SVG />
    </Icon>
  );
}
