import React from 'react';

import { Icon, TProps } from '../../Icon';
import { ReactComponent as SVG } from '../svg/Niue.svg';

export function Niue(props: TProps) {
  return (
    <Icon {...props} name="Niue">
      <SVG />
    </Icon>
  );
}
