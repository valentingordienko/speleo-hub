import React from 'react';

import { Icon, TProps } from '../../Icon';
import { ReactComponent as SVG } from '../svg/AlignRightOutline.svg';

export function AlignRightOutline(props: TProps) {
  return (
    <Icon {...props} name="AlignRightOutline">
      <SVG />
    </Icon>
  );
}
