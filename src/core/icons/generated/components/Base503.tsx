import React from 'react';

import { Icon, TBaseProps } from '../../Icon';
import { ReactComponent as SVG } from '../svg/Base503.svg';

export function Base503(props: TBaseProps) {
  return (
    <Icon {...props} name="Base503">
      <SVG />
    </Icon>
  );
}
