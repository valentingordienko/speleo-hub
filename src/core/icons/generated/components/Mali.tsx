import React from 'react';

import { Icon, TProps } from '../../Icon';
import { ReactComponent as SVG } from '../svg/Mali.svg';

export function Mali(props: TProps) {
  return (
    <Icon {...props} name="Mali">
      <SVG />
    </Icon>
  );
}
