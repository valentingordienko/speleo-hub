import React from 'react';

import { Icon, TProps } from '../../Icon';
import { ReactComponent as SVG } from '../svg/Yemen.svg';

export function Yemen(props: TProps) {
  return (
    <Icon {...props} name="Yemen">
      <SVG />
    </Icon>
  );
}
