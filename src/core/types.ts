export type TClassName = {
    className?: string
}
export type TDisabled = {
    disabled?: boolean
}
export type TReadonly = {
    readonly?: boolean
}
export type TProcessing = {
    processing?: boolean
}